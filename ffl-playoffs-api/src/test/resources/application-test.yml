spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/ffl-playoffs-test
      database: ffl-playoffs-test
      auto-index-creation: true

  # Use embedded MongoDB for tests
  mongodb:
    embedded:
      version: 4.0.2

google:
  oauth:
    client-id: test-client-id-for-testing

logging:
  level:
    com.ffl.playoffs: DEBUG
    org.springframework.data.mongodb: DEBUG
